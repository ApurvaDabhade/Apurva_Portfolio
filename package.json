{
  "name": "apurva-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Apurva Dabhade — Personal website (MERN stack)",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "npm run dev --prefix server & npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build:client": "npm install --prefix client && npm run build --prefix client",
    "vercel-build": "npm install --prefix client && npm run build --prefix client",
    "start": "NODE_ENV=production npm run start --prefix server",
    "seed": "npm run seed --prefix server"
  }
}
